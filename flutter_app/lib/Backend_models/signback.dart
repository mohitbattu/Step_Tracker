import 'package:flutter/material.dart';

class Signbackend {
  String fullname;
  String email;
  String password;
  String gender;
  
  Signbackend({this.fullname, this.email, this.password,this.gender});
}